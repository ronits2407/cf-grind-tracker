# External Libraries

The `Chart.js` library is used for creating visualizations on the dashboard. It is currently loaded directly from a CDN within `dashboard.html`.

If you prefer offline support, you can download the Chart.js bundle and place it here.
